#include "config.h"
#include <json-c/json.h>

bool load_config(config_t *config, const char *filename)
{
    // TODO:
    // - load file
    // - parse JSON
    // - validate JSON
    // - populate config_t
    return true;
}
