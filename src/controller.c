#include "controller.h"

bool controller_init(controller_t *controller, const config_t *const config)
{
    (void)controller;
    (void)config;
    return true;
}

void controller_run(controller_t *const controller)
{
    (void)controller;
    while (1)
        ;
}
